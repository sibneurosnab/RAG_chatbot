# ---------------------------
#  RAG-Telegram-LateChunk
#  env.example – скопируйте как .env и заполните
# ---------------------------

# ----- n8n / Telegram webhook
N8N_HOST=localhost              # внешний домен или IP (без протокола)
WEBHOOK_URL=http://localhost    # URL, на который Telegram будет слать webhooks
WEBHOOK_TUNNEL_URL=             # если используете ngrok/pages.dev – укажите здесь

# ----- Supabase
SUPABASE_URL=https://PROJECT.supabase.co
SUPABASE_SERVICE_KEY=           # service_role key (полный доступ)

# ----- API-ключи
OPENROUTER_API_KEY=             # https://openrouter.ai/ (LLM)
FIRECRAWL_API_KEY=              # optional – SaaS-scraper
DUMPLINGAI_API_KEY=             # optional – SaaS video transcription

# ----- Прочие настройки (оставьте дефолт, если не уверены)
N8N_PROTOCOL=http               # http|https, обычно http за reverse-proxy
NODE_ENV=production
N8N_LOG_LEVEL=info

# ----- Прокси (опц.)
#HTTP_PROXY=
#HTTPS_PROXY= 
